тесты дропдауна
тесты модального окна, timerSection, Timer

при редактировании todos текст пишется наоборот


ЛОГИКА ФОРМИРОВАНИЯ СТАТИСТИКИ:

при инициализации приложения проверка в local store
есть ли сегодняшняя дата,
если нет создать объект - ключ сегодняшняя дата

как работает фокус:
workTime - количество минут за работой всего
pauseTime - - количество минут за паузой всего

{
    date: currentDate,
    day: monday,
    stopsAndPauses: number,
    timeOnPause: seconds,
}

при любом событии происходит перезапись данных в локал сторе:

для этого проверяем есть ли объект по сегодняшнему ключу
если нет - создаем
из объекта берем данные
прибавляем к ним данные из редакс
данные редакс удаляем
