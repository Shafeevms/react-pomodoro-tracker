тесты дропдауна
тесты модального окна, timerSection, Timer

как тестить src/components/Modal/__tests__/index.test.tsx??

это нужно тестировать?:
export const todayDayAndWeek = (): string => dayjs().format('d-W');
export const currentWeek = (): number => +dayjs().format('W');
export const currentDay = (): number => dayjs().day();

при активном таймере при переходе на страницу статистики, должна ставится пауза?
при закрытии окна?

!!ОШИБКА!!

если удалить текущее дело (по которому активен таймер),
а до этого прибавить к нему время - время не сбрасывается.


это сделать как сервисворкер
будет в фоне, в браузере висеть и что-то делать - например, раз в час удалять старые данные накопившиеся

